# Placeholder images — DELETE WHEN REPLACED

30 stock photos (`placeholder-00.jpg` … `placeholder-29.jpg`, 1200×800, ~120KB
each) pulled from [Picsum Photos](https://picsum.photos) on 2026-09-02 for use
as filler while real content/photography is being sourced. Picsum serves real
Unsplash photos free, no API key or attribution required for this kind of
placeholder use (each has a real photographer + source URL, queryable via
`https://picsum.photos/id/{n}/info` if attribution is ever needed).

**These are not real site content.** As each one gets replaced by an actual
photo, delete the placeholder file. Once every reference to this directory is
gone, delete the whole folder.
